---
apiVersion: batch/v1
kind: CronJob
metadata:
  labels:
    app.kubernetes.io/name: segment-bridge
  name: segment-bridge
  namespace: segment-bridge
spec:
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - env:
                # Note: env values take Kubernetes precedence over envFrom (Secret).
                # To override TEKTON_RESULTS_CA_PATH, use an overlay, not the Secret.
                - name: TEKTON_RESULTS_CA_PATH
                  value: /var/run/segment-bridge/tekton-results-ca/service-ca.crt
              envFrom:
                - secretRef:
                    name: segment-bridge-config
                    optional: true
              image: quay.io/konflux-ci/segment-bridge:latest
              imagePullPolicy: IfNotPresent
              name: segment-bridge
              resources:
                limits:
                  cpu: 200m
                  memory: 256Mi
                requests:
                  cpu: 50m
                  memory: 64Mi
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop:
                    - ALL
                readOnlyRootFilesystem: true
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
                - name: tekton-results-service-ca
                  mountPath: /var/run/segment-bridge/tekton-results-ca
                  readOnly: true
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
          serviceAccountName: segment-bridge
          volumes:
            - name: tmp
              emptyDir: {}
            - name: tekton-results-service-ca
              configMap:
                name: segment-bridge-tekton-results-service-ca
                optional: true
                items:
                  - key: service-ca.crt
                    path: service-ca.crt
  schedule: 0 * * * *
