---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: segment-bridge
  name: segment-bridge
rules:
  - apiGroups:
      - ""
    resourceNames:
      - kube-system
    resources:
      - namespaces
    verbs:
      - get
  - apiGroups:
      - config.openshift.io
    resources:
      - clusterversions
    verbs:
      - get
      - list
  # fetch-konflux-op-records.sh: cluster-scoped Konflux CR "konflux"
  - apiGroups:
      - konflux.konflux-ci.dev
    resources:
      - konfluxes
    verbs:
      - get
      - list
  # get-konflux-public-info.sh: needs get on configmap/konflux-public-info in namespace konflux-info (granted by Konflux operator).
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/name: segment-bridge
  name: segment-bridge
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: segment-bridge
subjects:
  - kind: ServiceAccount
    name: segment-bridge
    namespace: segment-bridge
